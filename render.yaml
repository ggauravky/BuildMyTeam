services:
  - type: web
    name: buildmyteam-backend
    runtime: node
    rootDir: backend
    buildCommand: npm ci
    startCommand: npm start
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
      - key: MONGODB_URI
        sync: false
      - key: JWT_SECRET
        sync: false
      - key: JWT_EXPIRES_IN
        value: 7d
      - key: CLIENT_URL
        sync: false
      - key: MONGO_SERVER_SELECTION_TIMEOUT_MS
        value: 15000
      - key: DB_FALLBACK_TO_MEMORY
        value: true
      - key: ADMIN_SYNC_ON_STARTUP
        value: false
