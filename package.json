{
  "name": "buildmyteam-root",
  "private": true,
  "version": "1.0.0",
  "engines": {
    "node": ">=20 <25"
  },
  "scripts": {
    "build": "npm install --prefix frontend && npm run build --prefix frontend"
  }
}
